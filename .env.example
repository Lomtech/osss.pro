# ─────────────────────────────────────────────────────────────────────────────
# Osss — .env Vorlage
# ─────────────────────────────────────────────────────────────────────────────
# Kopiere diese Datei zu `.env.local` und fülle die echten Werte ein.
# .env-Files sind via .gitignore vom Repo ausgeschlossen.
#
# Audit 2026-05-11: ANALYTICS_SALT, IBAN_ENCRYPTION_KEY und CRON_SECRET
# dürfen in Production NIE leer/Default sein — sonst bricht das DSGVO-
# Anonymisierungsversprechen bzw. der Encryption-/Auth-Schutz.

# ─── Supabase ────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://YOURPROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...           # server-only, NIE NEXT_PUBLIC_

# ─── App-URL ─────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000          # Production: https://www.osss.pro

# ─── Stripe ──────────────────────────────────────────────────────────────────
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...                    # Platform-Webhook
STRIPE_CONNECT_WEBHOOK_SECRET=whsec_...            # Connect-Webhook (Account events)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_PLATFORM_FEE_PERCENT=0                      # Anteil platform-fee (0 = USP vs. MAAT)
STRIPE_CLIENT_ID=                                  # optional/legacy — OAuth-Connect; /api/stripe/status meldet nur, ob gesetzt

# Lifetime-Pilot-Coupon (Stripe Dashboard verwaltet — hier nur Dokumentation)
# STRIPE_LIFETIME_PILOT_COUPON=PILOT10             # 40% Rabatt forever, max 10 Slots

# ─── Email (Resend) ──────────────────────────────────────────────────────────
RESEND_API_KEY=re_...
RESEND_FROM_EMAIL=Osss <no-reply@osss.pro>
CONTACT_FORM_TO=                                   # Empfänger der /api/public/contact-Formulare (Kontakt/Demo)

# ─── Notifications ───────────────────────────────────────────────────────────
# true = payment-reminders schreiben in notification_queue (Worker-Pattern),
# leer/false = direkter Mail-Pfad. Siehe src/lib/notification-queue.ts.
NOTIFICATION_QUEUE_ENABLED=

# ─── Rate-Limit + Cache (Upstash Redis) ──────────────────────────────────────
UPSTASH_REDIS_REST_URL=https://YOUR.upstash.io
UPSTASH_REDIS_REST_TOKEN=AY...

# ─── Encryption + Secrets ────────────────────────────────────────────────────
# AES-256-GCM Key für members.bank_iban_enc — 64 Hex-Zeichen.
# Erzeugen: `openssl rand -hex 32`
IBAN_ENCRYPTION_KEY=                                # KRITISCH

# HMAC für OAuth state-Tokens (Stripe Connect Callback)
OAUTH_STATE_SECRET=                                 # min 32 Zeichen

# CRON_SECRET: Bearer-Token für /api/cron/* — min 16 Zeichen.
# Erzeugen: `openssl rand -base64 32`
CRON_SECRET=                                        # KRITISCH

# Analytics-Salt: täglich rotierender Hash-Salt für page_views.visitor_hash.
# Erzeugen: `openssl rand -base64 48`
# OHNE diesen ist die Hash-Anonymisierung in Production reversibel — DSGVO-Verstoß.
ANALYTICS_SALT=                                     # KRITISCH

# Optional: detailliertes Health-Check-Diagnose-Token (Header X-Health-Debug-Token)
HEALTH_DEBUG_TOKEN=                                 # leer = keine Diagnose

# ─── Admin-CRM ───────────────────────────────────────────────────────────────
ADMIN_EMAILS=                                       # komma-separiert, z.B. you@osss.pro,co@osss.pro

# ─── CSRF Origin-Allowlist (optional, komma-separiert) ───────────────────────
# CSRF_ALLOWED_ORIGINS=https://custom-domain.example

# ─── Sentry ──────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SENTRY_DSN=https://YOUR@sentry.io/PROJECT
# SENTRY_AUTH_TOKEN für Source-Map-Upload — nur in CI
SENTRY_AUTH_TOKEN=

# ─── Google Places (Lead-Enrichment, optional) ───────────────────────────────
GOOGLE_PLACES_API_KEY=

# ─── Phase 1+2: Bot-Defense + Observability + Feature-Flags ──────────────────
# Setup-Doku: docs/PHASE1_PHASE2_SETUP.md. Alle optional — ohne sie skippt der
# jeweilige Code sauber (Turnstile-Widget rendert nicht, OTel/GrowthBook no-op).
#
# Cloudflare Turnstile (unsichtbares CAPTCHA in /register, 5. Bot-Defense-Schicht):
NEXT_PUBLIC_TURNSTILE_SITE_KEY=                     # 0x4AAA... (client, public)
TURNSTILE_SECRET_KEY=                               # 0x4AAA... (server)
# OpenTelemetry → Hyperdx (Distributed Tracing):
HYPERDX_API_KEY=
OTEL_EXPORTER_OTLP_ENDPOINT=                        # optional: custom OTLP-Receiver (Grafana Tempo etc.)
OTEL_SERVICE_NAME=osss                              # Service-Name in Traces
# GrowthBook Feature-Flags (server + client lesen denselben Key):
GROWTHBOOK_SDK_KEY=                                 # sdk-...
NEXT_PUBLIC_GROWTHBOOK_SDK_KEY=                     # sdk-... (gleicher Wert)

# ─── Inkasso (Forderungsübergabe an externe Anbieter) ────────────────────────
# Provider-agnostisches Gerüst (src/lib/inkasso). Echte Anbieter (Finion FairPay,
# debtist, …) sind vertrags-/credential-gated und werden als Adapter ergänzt.
# Sandbox-Adapter zum End-to-End-Testen ohne echten Vertrag:
INKASSO_SANDBOX_ENABLED=                            # 'true' aktiviert den Sandbox-Provider (Slot 'other')
INKASSO_WEBHOOK_SECRET=                             # shared secret für /api/inkasso/webhook/<provider> (Header x-inkasso-secret)
# Paywise (paywise.de) — siehe docs/INKASSO_PAYWISE_MAPPING.md. Test- vs. Prod-Keys getrennt.
INKASSO_PAYWISE_API_TOKEN=                          # Case-Management-API (Debtors/Claims) — aktiviert den Provider 'paywise'
INKASSO_PAYWISE_PARTNER_TOKEN=                      # Partner-API (Company/User-Onboarding); Key braucht Permission API_ONLY_ONBOARDING
INKASSO_PAYWISE_WEBHOOK_SECRET=                     # HMAC-SHA256-Secret des Webhook-Endpoints (Header X-Paywise-Signature: sha256=…)
INKASSO_PAYWISE_BASE_URL=https://api.paywise.de     # API-Basis (Default; selten zu ändern)

# ─── Inngest (Background Jobs / Workflows) ───────────────────────────────────
# Dev: Inngest-CLI läuft lokal (npm run dev:inngest), Keys werden ignoriert.
# Prod: Keys aus dem Inngest-Dashboard (https://app.inngest.com/env/production/manage/signing-key).
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# ─── Smoke-Tests (optional, nur lokal) ───────────────────────────────────────
TEST_API_BASE=http://localhost:3000
TEST_OWNER_EMAIL=
TEST_OWNER_PASSWORD=
TEST_MEMBER_ID=
TEST_GYM_SLUG=
TEST_CRON_SECRET=
TEST_INACTIVE_PORTAL_TOKEN=
TEST_CRON_TRIGGER_ALLOWED=0                         # auf 1 setzen erlaubt cron-Trigger-Tests
